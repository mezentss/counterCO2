# 🤖 Архитектура Telegram-бота для расчета выбросов CO₂

## 📋 Обзор проблемы и решения

### ❌ **Проблема:**
- Ошибка `BadRequest: There is no text in the message to edit`
- Возникала при попытке отредактировать сообщение без текста

### ✅ **Решение:**
- Добавили блок `try-except` для `edit_message_text()`
- При ошибке используем `reply_text()` как запасной вариант
- Добавили параметр `show_keyboard` для функции `help_command`

## 🏗️ Детальная архитектура

### 📁 **Структура файлов:**

```
counterCO2/
├── bot/
│   └── telegram_bot.py          # Основной файл бота
├── api/
│   ├── co2_calculator.py       # Расчет CO₂ и расстояний
│   └── transport_logic.py      # Логика доступности транспорта
├── db/
│   ├── cities_database.py      # База городов и координат
│   ├── distance_service.py     # Сервис расчета расстояний
│   └── emission_calculator.py # Обертка для расчета выбросов
└── utils/
    └── visualizer.py          # Генерация графиков
```

### 🔄 **Поток выполнения операций:**

#### **1. Пользователь нажимает кнопку:**
```
Пользователь → Telegram → button_callback() → user_data[user_id] = {...}
```

#### **2. Пошаговый ввод городов:**
```
Шаг 1: user_data[user_id]['city_from'] = "Москва"
Шаг 2: user_data[user_id]['city_to'] = "Санкт-Петербург"
```

#### **3. Выполнение расчета:**
```
perform_calculate() → get_distance() → calculate_emissions() → результат
```

#### **4. Генерация графика:**
```
visualizer.py → matplotlib → байты изображения → Telegram
```

## 📊 **Подробное объяснение функций:**

### 🔧 **Основные функции бота:**

#### **`get_main_keyboard()`**
```python
def get_main_keyboard():
    keyboard = [
        [InlineKeyboardButton("📊 Рассчитать выбросы CO₂", callback_data='calculate')],
        [InlineKeyboardButton("🌱 Найти экологичный маршрут", callback_data='route_best')],
        [InlineKeyboardButton("📈 Сравнить маршруты", callback_data='compare')],
        [InlineKeyboardButton("❓ Справка", callback_data='help')]
    ]
    return InlineKeyboardMarkup(keyboard)
```
- Создает интерактивные кнопки
- `callback_data` - идентификатор действия

#### **`button_callback(update, context)`**
```python
async def button_callback(update, context):
    query = update.callback_query
    await query.answer()  # Закрывает анимацию кнопки
    
    if action == 'calculate':
        user_data[user_id] = {'action': 'calculate', 'step': 1}
        # Пытаемся отредактировать сообщение
        try:
            await query.edit_message_text("📊 Введите город отправления:")
        except:
            # Если не получилось - отправляем новое сообщение
            await query.message.reply_text("📊 Введите город отправления:")
```

#### **`handle_message(update, context)`**
```python
async def handle_message(update, context):
    user_id = update.effective_user.id
    message_text = update.message.text.strip()
    
    # Проверяем, начал ли пользователь операцию
    if user_id not in user_data:
        await update.message.reply_text("Выберите действие на кнопках:")
        return
    
    # Определяем текущее действие и шаг
    action = user_data[user_id]['action']
    step = user_data[user_id]['step']
    
    # Вызываем соответствующий обработчик
    if action == 'calculate':
        await handle_calculate_input(update, user_id, message_text, step)
```

### 📈 **Функции пошагового ввода:**

#### **`handle_calculate_input()`** (2 шага)
```python
async def handle_calculate_input(update, user_id, message_text, step):
    if step == 1:
        # Сохраняем город отправления
        user_data[user_id]['city_from'] = message_text
        user_data[user_id]['step'] = 2
        await update.message.reply_text("Введите город назначения:")
    
    elif step == 2:
        # Сохраняем город назначения и выполняем расчет
        user_data[user_id]['city_to'] = message_text
        await perform_calculate(update, user_id)
```

#### **`handle_compare_input()`** (4 шага)
```python
async def handle_compare_input(update, user_id, message_text, step):
    if step == 1:
        user_data[user_id]['city1_from'] = message_text
        user_data[user_id]['step'] = 2
    elif step == 2:
        user_data[user_id]['city1_to'] = message_text
        user_data[user_id]['step'] = 3
    # ... и так далее для 4 шагов
```

### ⚡ **Функции выполнения расчетов:**

#### **`perform_calculate()`**
```python
async def perform_calculate(update, user_id):
    city_from = user_data[user_id]['city_from']
    city_to = user_data[user_id]['city_to']
    
    # 1. Получаем расстояние
    distance_km = get_distance(city_from, city_to)
    
    # 2. Рассчитываем выбросы для каждого транспорта
    for transport_type in ['plane', 'train', 'car', 'bus']:
        emissions = calculate_emissions(transport_type, distance_km, city_from, city_to)
        # Добавляем в результаты
    
    # 3. Отправляем результат
    del user_data[user_id]  # Очищаем состояние
    await update.message.reply_text(result, reply_markup=get_main_keyboard())
```

## 🔗 **Взаимодействие модулей:**

### 📊 **Цепочка вызовов для расчета:**

```
1. bot/telegram_bot.py
   ↓
2. db/distance_service.py:get_distance()
   ↓
3. db/cities_database.py:get_city_info() (2 раза)
   ↓
4. api/co2_calculator.py:calculate_distance_direct()
   ↓
5. db/emission_calculator.py:calculate_emissions()
   ↓
6. api/co2_calculator.py:get_co2_emissions()
   ↓
7. utils/visualizer.py:create_*_chart()
   ↓
8. bot/telegram_bot.py (отправка результата)
```

### 💾 **Управление состоянием:**

```python
# Структура user_data:
user_data = {
    12345: {  # ID пользователя Telegram
        'action': 'calculate',     # Текущее действие
        'step': 2,               # Текущий шаг
        'city_from': 'Москва',     # Введенные данные
        'city_to': 'СПб'
    },
    67890: {  # Другой пользователь
        'action': 'compare',
        'step': 3,
        'city1_from': 'Казань',
        'city1_to': 'Сочи',
        'city2_from': 'Москва'
    }
}
```

## 🛡️ **Обработка ошибок:**

### 📝 **Типы ошибок:**

1. **`BadRequest`** - ошибка редактирования сообщения
2. **`ValueError`** - город не найден в базе
3. **`TransportUnavailableError`** - транспорт недоступен
4. **Общие исключения** - ошибки API, сети и т.д.

### 🔧 **Механизмы защиты:**

```python
# 1. Try-except для редактирования сообщений
try:
    await query.edit_message_text(text)
except:
    await query.message.reply_text(text)

# 2. Проверка состояния пользователя
if user_id not in user_data:
    await update.message.reply_text("Выберите действие:")
    return

# 3. Обработка ошибок расчета
try:
    distance_km = get_distance(city_from, city_to)
except ValueError as e:
    await update.message.reply_text(f"❌ Ошибка: {str(e)}")
    return
```

## 🎨 **Генерация визуализаций:**

### 📊 **Типы графиков:**

1. **Столбчатая диаграмма** - сравнение транспорта
2. **Круговые диаграммы** - сравнение двух маршрутов  
3. **Горизонтальная диаграмма** - подсветка лучшего варианта

### 🎨 **Цветовая схема:**
```python
colors = []
for emission in emissions:
    if emission <= 30:
        colors.append('#2ecc71')  # Зеленый - экология
    elif emission <= 60:
        colors.append('#f39c12')  # Оранжевый - средне
    elif emission <= 90:
        colors.append('#e67e22')  # Темно-оранжевый
    else:
        colors.append('#e74c3c')  # Красный - много выбросов
```

## 🚀 **Запуск и регистрация:**

### 📋 **Обработчики в main():**
```python
def main():
    application = Application.builder().token(token).build()
    
    # Команды
    application.add_handler(CommandHandler('start', start))
    application.add_handler(CommandHandler('help', help_command))
    
    # Кнопки
    application.add_handler(CallbackQueryHandler(button_callback))
    
    # Текстовые сообщения
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_message))
    
    application.run_polling()
```

## ✨ **Ключевые особенности реализации:**

1. **Интерактивность** - кнопки вместо команд
2. **Пошаговость** - контролируемый ввод данных
3. **Мультипользовательность** - изолированные состояния
4. **Отказоустойчивость** - обработка всех ошибок
5. **Визуализация** - автоматические графики
6. **Интуитивность** - понятный интерфейс

Теперь бот полностью готов и надежно работает! 🎉
